# Self-hosted font choices

- Inter (body, headings, and display) and IBM Plex Mono (status, timestamps, and terminal text) are copied from `@fontsource/*` packages.
- Both families are distributed under the SIL Open Font License through Fontsource/Google Fonts.
- The UI intentionally uses a restrained sans-serif system instead of novelty display faces.
